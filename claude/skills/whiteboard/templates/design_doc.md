Date: YYYY-MM-DD
Status: Draft <!-- Draft | Approved — flips to Approved only once the user gives an explicit final decision and Open Questions is empty -->

# <Title>

## Requirements
The outcome to achieve, why it's needed, and the concrete conditions that define success. Fold in any essential background or existing systems the reader needs to make sense of it.

## Scope
In scope: what this work covers. Out of scope: what it deliberately does not, so execution doesn't drift into it.

## Design Decision
The chosen design and the specific reasoning that made it win over the alternatives (name them and why they lost). Tag claims that rest on research with `[Finding N]`.

## Risks / Trade-offs
What could break, be slow, or need a follow-up. Tag claims that rest on research with `[Finding N]`.

## Tasks
Ordered, checkable steps. Each `- [ ]` is self-contained — the action, the file(s) it touches, and how to confirm it's done — so it can be executed without re-reading the whole doc. Check each off (`- [x]`) as completed so progress survives compaction or a new session.

## Verification Plan
What proves this done: the test coverage to add and the manual sanity-checks to run.

## Open Questions
Anything unresolved that needs a decision before/during implementation. Empty before Status flips to Approved.

## Findings
What the sources show, in plain terms, for every claim the Design Decision or Risks rests on. Number each (`Finding 1`, `Finding 2`, ...) so they can be cited directly. A conflicting or unresolved source is its own Finding, stated as such.

### Sources
- <source name>: <url or file path>, what it confirms or refutes.
